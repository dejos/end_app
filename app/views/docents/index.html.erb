<h1>Docenten</h1>

<table>
  <tr>
    <th>First name</th>
    <th>Last name</th>
    <th>Email</th>
    <th>Password</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @docents.each do |docent| %>
  <tr>
    <td><%= docent.first_name %></td>
    <td><%= docent.last_name %></td>
    <td><%= docent.email %></td>
    <td><%= docent.password %></td>
    <td><%= link_to 'Show', docent %></td>
    <td><%= link_to 'Edit', edit_docent_path(docent) %></td>
    <td><%= link_to 'Destroy', docent, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Docent', new_docent_path %>
<a href="http://localhost:3000/">Terug</a>
